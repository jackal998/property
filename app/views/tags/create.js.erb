$('#tags').append(' <%= link_to "##{@tag.name}", tag_path(@tag), :class=>"btn btn-link", :style=>"padding: 0;" if @tag %>');
$('#tag_input').val('');
$('#tags').find('a').attr({'data-method':'get', 'class':'btn btn-link'});
// $('#tags').find('a').addClass('btn-link');
// $('#tags').find('a').removeClass('btn-danger');
