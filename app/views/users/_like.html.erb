<% if liked?(obj.id) %>
  <% class_of_like_link = "btn glyphicon glyphicon-star" %>
<% else %>
  <% class_of_like_link = "btn glyphicon glyphicon-star-empty" %>
<% end %>
<%= link_to obj.user_collectionships.size, edit_user_path(obj.id), :class => class_of_like_link, :remote => true %>